# 커뮤니케이션 규칙

- 사용자에게 응답(대화 메시지)할 때는 항상 한국어로 답한다.
- 코드, 커밋 메시지, 파일 내 주석, 변수/파일명 등 실제 작업물은 기존처럼 영어로 작성해도 된다 (이 프로젝트의 기존 관례를 따름). 이 규칙은 사용자와 대화할 때 쓰는 언어에만 적용된다.

# 브랜치 / 워크플로우

- `main`이 이 저장소의 메인 브랜치다. 사용자의 로컬이 이제 주 작업 환경이므로, 새 작업은 `main`에서 분기하거나 `main`에 직접 커밋한다.
- `claude/boxandcox-website-4pbxgk`는 예전 세션에서 쓰던 브랜치로, `main`과 커밋 히스토리가 동일한 시점에서 갈라졌다. 더 이상 기본 브랜치로 취급하지 않는다.

# 애셋 커밋 규칙

- `assets/` 아래에 새 파일을 추가하거나 수정할 때는, 실제로 HTML/CSS/JS에서 참조되어 사이트에 적용된 파일만 커밋한다. 원본/미사용 소스 파일을 저장소에 그대로 커밋하지 않는다.
- 이 저장소에는 `scripts/git-hooks/pre-commit` 훅이 있다. 로컬에서 한 번 `git config core.hooksPath scripts/git-hooks`로 활성화해두면, 300KB가 넘는 새 애셋 파일은 커밋이 자동으로 막힌다. 데스크탑에서 의도적으로 실제 애셋을 추가/교체할 때만 `ALLOW_ASSET_COMMIT=1 git commit -m "..."`처럼 예외적으로 허용한다.
- 모바일 등 가벼운 세션에서 푸시할 때는 새 애셋 파일을 추가하지 말고, `js/data.js`에서 이미 저장소에 있는 애셋들 중 어떤 걸 어디에 쓸지 재배치(셔플)하는 정도로만 작업한다. 위 훅이 실수로 큰 파일이 섞여 들어가는 것을 1차로 막아주지만, 모든 모바일 클라이언트가 git hook을 실행하는 건 아니므로 이건 안전망이지 완벽한 강제 수단은 아니다 — 실제로는 "모바일에서는 새 애셋을 추가하지 않는다"는 관례를 지키는 게 핵심이다.
